version: 2
jobs:
  build:
    docker:
      - image: buildpack-deps:stable-scm
    steps:
      - checkout
      - run: bash -e .circleci/get-deps.sh
      - run: bash -e .circleci/build-hugo.sh
      - restore_cache:
          key: v2-dependencies_-{{ checksum "package.json" }}
      - run: npm ci
      - save_cache:
          key: v2-dependencies_-{{ checksum "package.json" }}
          paths:
            - ~/.npm
      - run: bash -e .circleci/git-init.sh
      - run: npm start
      - run: bash -e .circleci/deploy.sh